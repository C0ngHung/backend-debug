/**
 * Auth entity package — JPA entities for authentication module.
 *
 * Marked as @NullMarked (Jspecify) to match Spring Framework 7's
 * null-safety contract. All types are non-null by default unless
 * explicitly annotated with @Nullable.
 */
@NullMarked
package smartosc.conghung.modules.auth.entity;

import org.jspecify.annotations.NullMarked;
